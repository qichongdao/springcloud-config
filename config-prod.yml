server:
  port: 80
spring:
  application:
    name: cloud-consumer-order-prod
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
